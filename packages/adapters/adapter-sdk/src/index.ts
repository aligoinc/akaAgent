/**
 * @akabiz/adapter-sdk — shared utilities cho service adapters.
 *
 * Phase 0 (current): placeholder.
 * Future:
 *   - AuthFlow (OAuth2 PKCE, refresh, API key validation)
 *   - RateLimiter (token bucket per service)
 *   - HttpClient (with retry + tracing)
 *   - WebhookValidator (HMAC signature verify)
 */

export const SDK_VERSION = '0.0.0'
