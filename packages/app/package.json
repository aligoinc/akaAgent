{
  "name": "@akabiz/app",
  "version": "0.0.0",
  "description": "akaBiz Auto v2 — Electron + React app services + CLI runner.",
  "private": true,
  "type": "module",
  "main": "./dist/cli/run-workflow.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(no tests yet)\" && exit 0",
    "run-workflow": "npm run build && node dist/cli/run-workflow.js",
    "seed-workflow": "npm run build && node dist/cli/seed-workflow.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@akabiz/engine": "*",
    "@supabase/supabase-js": "^2.49.1",
    "croner": "^9.1.0",
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3"
  }
}
